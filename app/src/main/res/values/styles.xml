<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.Ab">
        <item name="android:homeAsUpIndicator">@drawable/ic_up_dark</item>
        <item name="homeAsUpIndicator">@drawable/ic_up_dark</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="AppTheme.OverlayedActionBar" parent="AppTheme">
        <item name="windowActionBarOverlay">true</item>
        <item name="android:windowActionBarOverlay">true</item>
    </style>

</resources>
